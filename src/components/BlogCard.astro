---
import type { Blog } from '../lib/types';

interface Props {
  article: Blog;
}

const { article } = Astro.props;
const href = article.slug ? `/blog/${article.slug}` : '#';

// Reading time estimate from excerpt word count (fallback for listing page)
const text = (article.excerpt || article.meta_description || '').replace(/<[^>]*>/g, '').trim();
const estimatedWords = text.split(/\s+/).filter(Boolean).length;
// Articles on the listing page don't have full HTML; estimate ~5 min average
const readingTime = Math.max(3, Math.ceil(estimatedWords / 30));

const formattedDate = new Date(article.published_at).toLocaleDateString('en-IN', {
  year: 'numeric',
  month: 'short',
  day: '2-digit',
});
---

<a href={href} class="group flex flex-col bg-white rounded-xl shadow-sm hover:shadow-lg transition-all duration-300 overflow-hidden border border-gray-100 h-full">
  <div class="aspect-[16/9] bg-gray-100 overflow-hidden flex-shrink-0">
    {article.featured_image ? (
      <img
        src={article.featured_image}
        alt={`${article.title} — Home Nesto`}
        class="w-full h-full object-cover group-hover:scale-[1.03] transition-transform duration-300"
        loading="lazy"
        width="800"
        height="450"
      />
    ) : (
      <div class="w-full h-full flex items-center justify-center text-gray-300">
        <svg class="w-10 h-10" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z" />
        </svg>
      </div>
    )}
  </div>

  <div class="p-5 flex flex-col flex-1">
    <div class="text-[10px] uppercase font-bold text-red-600 mb-2 tracking-wider">
      {article.sub_category || article.category || article.tags?.[0] || 'Real Estate'}
    </div>

    <h3 class="text-lg font-bold text-gray-900 group-hover:text-red-700 transition-colors line-clamp-2 leading-tight mb-3">
      {article.title}
    </h3>

    <div class="mt-auto pt-4 border-t border-gray-50 flex flex-wrap items-center gap-y-1 text-[11px] text-gray-400 font-medium">
      <time datetime={article.published_at}>
        {formattedDate}
      </time>
      <span class="mx-2 text-gray-300">•</span>
      <span>{readingTime} min read</span>
      <span class="mx-2 text-gray-300">•</span>
      <span class="text-gray-500">By {article.author?.full_name || article.author_name || 'Admin'}</span>
    </div>
  </div>
</a>
